spring:
  application:
    name: ms-transfer-bank
  profiles:
    active: ${ENVIRONMENT}
  cloud:
    config:
      uri: ${CONFIG_URI}
      fail-fast: true
      retry:
        max-attempts: 20
        max-interval: 15000
        initial-interval: 10000
      profile: ${ENVIRONMENT}